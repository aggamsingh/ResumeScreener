# CPU-only PyTorch index for lightweight Render deployment (<200MB RAM)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu

# Core API
fastapi~=0.111.0
uvicorn[standard]~=0.30.0
pydantic-settings~=2.3.0
python-multipart~=0.0.9
python-dotenv~=1.0.0

# Embedding model
sentence-transformers~=3.0.0

# Vector database client
qdrant-client~=1.9.0

# Document parsing — text-layer PDFs and DOCX
pdfplumber~=0.11.0
python-docx~=1.1.0

# OCR — for scanned / image-only PDFs
# Requires system packages: tesseract-ocr, poppler-utils (installed in Dockerfile)
# Set ENABLE_OCR=false in .env to skip OCR and save startup time if not needed
pytesseract~=0.3.10
pdf2image~=1.17.0
Pillow>=11.3.0

# LLM rerankers (both installed; only the configured one is called at runtime)
groq~=0.9.0
# Gemini is called over plain REST with `requests` (see api/reranker._call_gemini):
# the google-generativeai SDK pulls in grpc, which mock_grpc.py has to stub out
# here, and the two are not compatible. No SDK needed.

# Local JD generation (Qwen2.5-0.5B) — api/local_llm.py imports transformers
# directly. It arrived transitively via sentence-transformers, so a resolver
# change could have dropped it and broken /api/v1/generate-jd only at runtime.
transformers~=4.44

# HTTP utilities
httpx~=0.27.0
requests~=2.32
psycopg2-binary~=2.9.9
